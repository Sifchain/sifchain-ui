import { Vuextra } from "../Vuextra";

export const exportStore = Vuextra.createStore({
  options: {
    devtools: true,
  },
  state: {},
  getters: (state) => ({}),
  actions: (ctx) => ({}),
  mutations: (state) => ({}),
  modules: [],
});
