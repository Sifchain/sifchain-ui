import { Vuextra } from "../Vuextra";

export const exampleStore = Vuextra.createStore({
  options: {
    devtools: true,
  },
  state: {},
  getters: (state) => ({}),
  actions: (ctx) => ({}),
  mutations: (state) => ({}),
  modules: [],
});
